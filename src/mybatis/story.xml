<?xml version="1.0" encoding="UTF-8"?>
<!-- user.xml -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Story">

	<select id="getCount" resultType="int">
		select count(*) from yas_story_board
	</select>
	
	
	<select id="getArticle" parameterType="int" resultType="story.StoryDataBean"> 
		select story_member_id from yas_story_board where story_num = #{story_num};
	</select>
 
 	<!-- 
	<select id="maxNum" resultType="int">
		select max(num) from board
	</select>

	<update id="updateReply" parameterType="story.BoardDataBean">
		update board set re_step=re_step+1 where ref = #{ref} and re_step &gt; #{re_step}	
	</update> -->

	<insert id="insertArticles" parameterType="story.StoryDataBean">
		insert into yas_story_board( story_num, story_title, story_member_id, story_img)
		 values (yas_story_num.NEXTVAL, #{story_title}, #{story_member_id}, #{story_img,jdbcType=VARCHAR} )
	</insert>
	
	<select id="getList" parameterType="java.util.Map" resultType="story.StoryDataBean">
		<!-- select * from yas_story_board order by story_num desc -->
		select * from (
			select a.*, ROWNUM as r from(
				select * from yas_story_board order by story_num desc
			)
		a) where r &gt;= #{start}  and r &lt;= #{end}
	</select>
	
	<!-- <update id="addCount" parameterType="int">
		update board set readcount=readcount+1 where num=#{num}	
	</update>
	
	<update id="modifyArticle" parameterType="board.BoardDataBean">
		update board set email=#{email, jdbcType=VARCHAR}, 
		subject=#{subject}, content=#{content}, passwd=#{passwd} where num=#{num}	
	</update>
	
	<select id="checkReply" parameterType="board.BoardDataBean" resultType="int">
		select count(*) from board where ref=#{ref} and re_step=#{re_step}+1 and re_level &gt; #{re_level}		
	</select>
	
	<update id="delArticle" parameterType="int">
		update board set subject='삭제된 글입니다', readcount = -1 where num = #{num}
	</update>
	
	<update id="deleteReply" parameterType="board.BoardDataBean">
		update board set re_step=re_step-1 where ref = #{ref} and re_step &gt; #{re_step}
	</update>
	
	 -->
	<delete id="deleteArticles" parameterType="int">
		delete from yas_story_board where story_num = #{story_num}	
	</delete>
</mapper>  





















